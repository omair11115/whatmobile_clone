# Frontend Environment Variables (Vite)
# Prefix with VITE_ to expose to client-side if needed
VITE_API_BASE_URL=http://localhost:3000

# Backend URL for direct access if proxy isn't used
VITE_BACKEND_URL=http://localhost:3001

# Gemini API Key (handled by Vite define in config)
GEMINI_API_KEY=
